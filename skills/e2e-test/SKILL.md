---
name: e2e-test
description: >
  Web 端到端测试方法论。当需要对网页执行自动化测试、验证用户场景、
  或发现页面 bug 时使用本 skill。涵盖测试规划、执行流程、bug 分类与报告格式。
---

# E2E Test Skill

<purpose>

Web 端到端测试的方法论与执行流程。

分工：
- **本 skill**：定义"怎么测"——测试规划、执行循环、bug 分类、报告格式。
- **playwright-cli skill**：定义"怎么用工具"——命令语法、参数、示例。

</purpose>

<input-interpretation>

## 理解测试输入

支持两种输入：

| 输入形式 | 示例 | 处理方式 |
|----------|------|----------|
| 模糊描述 | "测试购票流程" | 自主分解为具体步骤后执行 |
| 结构化步骤 | `[{type, text}]` 列表 | 逐步执行并验证 |

收到模糊描述时，按以下策略分解：
1. 理解业务意图——用户想验证什么？
2. 推断前置条件——需要先做什么才能开始？
3. 拆解为 导航 → 交互 → 验证 的操作序列
4. 执行中根据实际页面状态动态调整——不要死板遵循预设步骤

</input-interpretation>

<execution-cycle>

## 测试执行循环

对每个测试步骤，严格执行以下循环：

```
观察 → 规划 → 执行 → 验证 → 记录
```

1. **观察**：snapshot 获取当前页面状态，了解可用元素
2. **规划**：基于页面状态确定最小必要动作（一次只做一件事）
3. **执行**：通过 playwright-cli 执行单个动作
4. **验证**：重新 snapshot，确认业务目标是否达成
5. **记录**：无论成败，记录执行结果与证据

### 关键判断规则

- 命令返回成功 ≠ 步骤通过。必须验证页面状态确实符合预期。
- 步骤通过的唯一标准是**业务目标达成**，不是命令执行完毕。
- 对联想框、下拉框、日期控件等复杂组件：操作后必须验证选中值是否正确，不能只依赖 fill。

</execution-cycle>

<bug-taxonomy>

## Bug 分类与主动发现

测试过程中，在完成场景步骤的同时，**主动检查以下类别**：

| 类别 | 关注点 | 发现方法 |
|------|--------|----------|
| 功能缺陷 | 核心功能不工作、流程中断、数据错误 | 操作后验证结果是否符合预期 |
| UI / 布局 | 元素错位、文字截断、遮挡、响应式异常 | snapshot / screenshot 观察 |
| 导航错误 | 死链接、错误重定向、返回行为异常 | 点击后检查 URL 与页面内容 |
| 输入验证 | 未校验非法输入、边界值处理不当 | 尝试异常输入观察反应 |
| 错误处理 | 无错误提示、误导性提示、静默失败 | 触发错误场景后观察页面反馈 |
| 性能问题 | 加载超时、操作卡顿、资源加载失败 | 关注操作响应时间与 network |
| 可访问性 | 缺少 alt 文本、键盘不可达、对比度不足 | snapshot 检查元素属性 |
| 控制台异常 | JS 错误、网络请求失败、资源 404 | console 与 network 输出 |

**发现原则**：
- 每个关键步骤执行后检查 console，留意错误和警告
- 关注页面加载和交互响应是否合理
- 通过 snapshot 留意视觉与结构异常
- 不需要刻意覆盖所有类别——基于实际观察自然发现

</bug-taxonomy>

<browser-workflow>

## 浏览器会话流程

```
open --headed → [ 执行循环 × N ] → 收集最终证据 → close
```

证据收集手段（按需选用）：

| 手段 | 用途 | 优先级 |
|------|------|--------|
| snapshot | 页面结构与元素状态 | 首选，轻量 |
| screenshot | 视觉证据，适用于 UI 问题 | 发现 UI 异常时使用 |
| console | JS 错误、警告、日志 | 每步检查 |
| network | 请求失败、异常状态码 | 可疑时检查 |

命令语法参考 **playwright-cli skill**，此处不重复。

</browser-workflow>

<report-format>

## 测试报告格式

最终输出**结构化 Markdown 报告**，包含以下部分：

### 1. 测试概况

- 目标 URL
- 测试场景描述
- 总体结论：✅ 通过 / ⚠️ 部分通过 / ❌ 未通过

### 2. 测试步骤详情

对每个执行的步骤：

> **步骤 N**：{步骤描述}
> - 状态：✅ 通过 / ❌ 失败 / ⚠️ 存疑
> - 执行摘要：做了什么、观察到什么
> - 证据：关键 snapshot 片段、命令输出

### 3. 发现的问题

对每个发现的问题：

> **[类别] 问题标题**
> - 严重程度：🔴 严重 / 🟡 中等 / 🟢 轻微
> - 描述：具体问题是什么
> - 复现路径：如何触发
> - 证据：直接来自浏览器的观察

若无问题发现，注明"未发现问题"。

### 4. 最终页面状态

测试结束时的页面 URL、标题和关键状态描述。

</report-format>

<constraints>

## 约束

- 不要跳过任何输入步骤——每一步都必须尝试执行并报告结果。
- 不要在初始观察后就停止——必须完成所有步骤的完整执行循环。
- 不要编造未观察到的现象——所有结论必须有 playwright-cli 输出作为证据。
- 不要重复 playwright-cli skill 的命令参考——需要时引导用户查阅该 skill。

</constraints>
